{
  "name": "changara-star-academy",
  "version": "3.0.0",
  "description": "Changara Star Academy Management System - Static site for Cloudflare Pages & Netlify, API on Cloudflare Workers",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "dev:worker": "cd worker && node node_modules/wrangler/bin/wrangler.js dev",
    "start": "node server.js",
    "build": "echo 'Static multi-page site — no build step required. Netlify/CF Pages publish directory = .'",
    "build:static": "echo 'Static multi-page site — no build step required. Netlify/CF Pages publish directory = .'",
    "deploy:worker": "cd worker && node node_modules/wrangler/bin/wrangler.js deploy",
    "deploy:pages": "echo 'Deploy via Cloudflare Pages UI (publish directory = .)'",
    "deploy:netlify": "npx netlify deploy --dir=. --prod",
    "lint": "echo 'No lint step configured'",
    "typecheck": "echo 'No typecheck step configured'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "school",
    "management",
    "education",
    "attendance",
    "student",
    "teacher",
    "cms",
    "cloudflare",
    "netlify",
    "cloudinary"
  ],
  "author": "Changara Star Academy",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^3.100.0"
  },
  "dependencies": {}
}
