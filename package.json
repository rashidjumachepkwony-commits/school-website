{
  "name": "changara-star-academy",
  "version": "2.0.0",
  "description": "Changara Star Academy Management System - Cloudflare Pages + D1",
  "main": "functions/api/[[path]].js",
  "scripts": {
    "start": "wrangler dev",
    "dev": "wrangler dev",
    "build:static": "node scripts/build-static.js",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create csa-school-db",
    "d1:migrate": "wrangler d1 execute DB --file=schema.sql",
    "d1:reset": "wrangler d1 execute DB --command='DROP TABLE IF EXISTS audit_logs; DROP TABLE IF EXISTS staff_attendance; DROP TABLE IF EXISTS student_attendance; DROP TABLE IF EXISTS visitors; DROP TABLE IF EXISTS content; DROP TABLE IF EXISTS students; DROP TABLE IF EXISTS staff; DROP TABLE' && wrangler d1 execute DB --file=schema.sql",
    "d1:seed": "node scripts/seed-admin.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "import:students": "node scripts/import-students.js",
    "import:students:dry": "node scripts/import-students.js --dry-run"
  },
  "keywords": [
    "school",
    "management",
    "education",
    "attendance",
    "student",
    "teacher",
    "cms",
    "cloudflare"
  ],
  "author": "Changara Star Academy",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^3.100.0"
  }
}
