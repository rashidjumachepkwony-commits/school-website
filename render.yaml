services:
  - type: web
    name: csa-school-system
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 5000
      - key: MONGODB_URI
        value: mongodb://localhost:27017/schoolDB