{
  "name": "csa-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "echo 'No lint step configured'",
    "typecheck": "echo 'No typecheck step configured'"
  },
  "devDependencies": {
    "wrangler": "^4.136.2"
  },
  "dependencies": {
    "mongodb": "^6.3.0"
  }
}
